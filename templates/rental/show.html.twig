{% extends 'base.html.twig' %}

{% block title %}Rental{% endblock %}

{% block body %}
<div class="container container-fluid mb-4">
<h2 class="page-header mt-4"> Cleaning Equipment </h2>
<br>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rental.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ rental.Name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ rental.Image }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ rental.Date ? rental.Date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ rental.Email }}</td>
            </tr>
            <tr>
                <th>Website</th>
                <td>{{ rental.Website }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rental_index') }}">back to list</a>

    <a href="{{ path('rental_edit', {'id': rental.id}) }}">edit</a>

    {{ include('rental/_delete_form.html.twig') }}
    </div>
{% endblock %}
